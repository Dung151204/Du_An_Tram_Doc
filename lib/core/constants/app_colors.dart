import 'package:flutter/material.dart';

class AppColors {
  // Màu nền chính (Xám rất nhạt)
  static const Color background = Color(0xFFF8F9FA);

  // Màu chữ
  static const Color textDark = Color(0xFF0F172A); // Đen xanh (Slate 900)
  static const Color textGrey = Color(0xFF94A3B8); // Xám nhạt (Slate 400)

  // Màu chủ đạo
  static const Color primary = Color(0xFF0F172A);

  // Màu điểm nhấn (Nút cộng, Thanh tiến độ)
  static const Color amber = Color(0xFFF59E0B);

  // Màu trắng
  static const Color white = Colors.white;
}